type Dictionaries = {
  "server-component": {header: {links: DictionariesLinks}};
};

type Header = {
  header: DictionariesLinks;
};

type DictionariesLinks = {
  home: string;
  services: string;
  projects: string;
  technologies: string;
  ourTeam: string;
  contact: string;
};
